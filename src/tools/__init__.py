"""Tool integration package.

The current implementation uses deterministic local adapters so the Agent
pipeline can run during development. Replace these adapters with real CRM,
ERP/Oracle, Elasticsearch, and Jira clients when integrating production systems.
"""
